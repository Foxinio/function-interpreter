#pragma once


#include "Parsing.h"
#include "ValidationRule.h"

namespace Parsing {
	std::list<ValidatingContainer> validate(std::list<std::string> list) {
		std::list<ValidatingContainer> result{};
		int bracketCounter = 0;
		for (auto& string : list) {

		}

		return result;
	}
}
