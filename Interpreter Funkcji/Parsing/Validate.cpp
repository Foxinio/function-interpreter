#pragma once


#include "Parsing.h"
#include "ValidatedElement.h"
namespace Parsing {
	std::list<ValidatingContainer> validate(std::list<std::string> list) {

		return std::list<ValidatingContainer>();
	}
}
